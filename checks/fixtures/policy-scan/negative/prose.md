This policy note can discuss fallback, legacy paths, shadow paths, and dual-write
patterns without creating a code finding.

```js
const example = "fallback and legacy wording inside a string";
// catch (error) { return null; }
```
